package BerlinClock.BugHunt;

public class Problem2 {
    // 2nd problem - ternar operation asking if it s exactly one element in the array tho
    // getting the second, we need to get the first element

//        public double[] computeMean(){
//            return points.size() == 1 ? points.get(0) : null;
//        }
}
